// 初始化变量i为1
let i = 1
// 使用while循环，当i小于等于5时执行循环体
while (i <= 5) {
    // 判断i是否等于3
    if (i === 3) {
        // 如果i等于3，则i自增1
        i++
        // 使用continue跳过本次循环的剩余部分
        continue  
    }
    // 打印我要吃第x个包子的信息，其中x为当前i的值
    console.log(`我要吃第${i}个包子`)
    // i自增1，进入下一次循环
    i++
}